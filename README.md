# oclif-workspace

```bash
yarn install
yarn workspace @oclif-workspace/lib build
yarn workspace @oclif-workspace/oclif-cli dev foo:bar
yarn workspace @oclif-workspace/oclif-cli oclif pack tarballs
```